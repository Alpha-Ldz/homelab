apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage-data
  namespace: stf
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-path  # Ajuster selon votre storageClass
